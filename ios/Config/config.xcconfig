PRODUCT_BUNDLE_IDENTIFIER = $(BUNDLE_IDENTIFIER)$(BUNDLE_IDENTIFIER_APPEND)

BUNDLE_DISPLAY_NAME = MobileSSOSessionExpiry
BUNDLE_IDENTIFIER = myapp.mobilessosessionexpiry

RUNTIME_URL = https:\/\/mobilessosessionexpiry-sandbox.mxapps.io/

PRODUCT_NAME = MobileSSOSessio
